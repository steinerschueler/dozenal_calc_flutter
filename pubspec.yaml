name: dozenal_calc_flutter
description: "Scientific calculator that computes natively in base 12 (dozenal), with custom digit glyphs, exact rational arithmetic, period detection, and an embedded didactic chapter set."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.1.0+14

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  flutter_markdown_plus: ^1.0.7
  shared_preferences: ^2.3.3
  url_launcher: ^6.3.1
  package_info_plus: ^8.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Launcher-icon and splash-screen generators. The source PNG at
  # assets/icon.png is produced by tool/generate_icon.dart
  # (`flutter test tool/generate_icon.dart`).
  #
  # After updating the source asset, regenerate platform files via:
  #   dart run flutter_launcher_icons
  #   dart run flutter_native_splash:create
  flutter_launcher_icons: ^0.14.0
  flutter_native_splash: ^2.4.0

flutter_launcher_icons:
  image_path: "assets/icon.png"
  android: true
  ios: true
  adaptive_icon_background: "#1F1F1F"
  adaptive_icon_foreground: "assets/icon.png"
  web:
    generate: true
    background_color: "#1F1F1F"
    theme_color: "#1F1F1F"
  remove_alpha_ios: true

flutter_native_splash:
  color: "#1F1F1F"
  image: "assets/icon.png"
  android_12:
    color: "#1F1F1F"
    image: "assets/icon.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Drives flutter gen-l10n from lib/l10n/*.arb on every `flutter pub get`.
  # Output classes land in lib/l10n/ (non-synthetic — discoverable in IDE,
  # listed in source control via .gitignore for build artifacts).
  generate: true

  assets:
    - assets/icon.png
    - assets/intro/
    - assets/theory/
    # Bundled so the SIL OFL 1.1 text ships with the JetBrains Mono font.
    - assets/fonts/OFL-JetBrainsMono.txt
    - legal/privacy-policy.de.md
    - legal/privacy-policy.en.md
    - legal/privacy-policy.fr.md
    - legal/privacy-policy.es.md
    - legal/privacy-policy.it.md
    - legal/privacy-policy.fa.md
    - legal/privacy-policy.ru.md
    - legal/privacy-policy.ga.md
    - legal/privacy-policy.hi.md
    - legal/privacy-policy.zh.md
    - legal/privacy-policy.zh-Hant.md
    - legal/privacy-policy.cy.md
    - legal/privacy-policy.ja.md
    - legal/privacy-policy.ar.md
    - legal/license.de.md
    - legal/license.en.md
    - legal/license.fr.md
    - legal/license.es.md
    - legal/license.it.md
    - legal/license.fa.md
    - legal/license.ru.md
    - legal/license.ga.md
    - legal/license.hi.md
    - legal/license.zh.md
    - legal/license.zh-Hant.md
    - legal/license.cy.md
    - legal/license.ja.md
    - legal/license.ar.md

  # Bundled true monospace font (JetBrains Mono, SIL OFL 1.1). The logical
  # 'monospace' family is only a real fixed-width font on Android; on
  # iOS/macOS/web it falls back to the proportional system font, which made
  # the monospace theory/conversion tables drift column-to-column. Bundling
  # guarantees fixed-width alignment on every platform.
  fonts:
    - family: JetBrainsMono
      fonts:
        - asset: assets/fonts/JetBrainsMono-Regular.ttf
        - asset: assets/fonts/JetBrainsMono-Bold.ttf
          weight: 700

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
